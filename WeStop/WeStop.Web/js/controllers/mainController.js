angular.module('WeStop').controller('mainController', ['$scope', function($scope) {

    // $scope.text = 'Olá';

    // let connection = new signalR.HubConnectionBuilder()
    //     .withUrl("http://localhost:5000/gameroom")
    //     .build();

    // connection.on("connected", data => {
    //     console.log(data);
    // });

    // connection.on("timePassed", data => {
    //     console.log('timer');
    // });

    // // connection.start()
    // //     .then(() => connection.invoke("Connect", {
    // //         name: '1234567',
    // //         password: ''
    // //     }));

    // connection.start()
    //     .then(() => connection.invoke('start_game'));

}]);